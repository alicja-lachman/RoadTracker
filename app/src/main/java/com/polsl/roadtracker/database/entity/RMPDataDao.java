package com.polsl.roadtracker.database.entity;

import java.util.List;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.query.Query;
import org.greenrobot.greendao.query.QueryBuilder;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "RMPDATA".
*/
public class RMPDataDao extends AbstractDao<RMPData, Void> {

    public static final String TABLENAME = "RMPDATA";

    /**
     * Properties of entity RMPData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Timestamp = new Property(0, Long.class, "timestamp", false, "TIMESTAMP");
        public final static Property Value = new Property(1, float.class, "value", false, "VALUE");
        public final static Property RouteId = new Property(2, Long.class, "routeId", false, "ROUTE_ID");
    }

    private Query<RMPData> routeData_RmpDataListQuery;

    public RMPDataDao(DaoConfig config) {
        super(config);
    }
    
    public RMPDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"RMPDATA\" (" + //
                "\"TIMESTAMP\" INTEGER," + // 0: timestamp
                "\"VALUE\" REAL NOT NULL ," + // 1: value
                "\"ROUTE_ID\" INTEGER);"); // 2: routeId
        // Add Indexes
        db.execSQL("CREATE INDEX " + constraint + "IDX_RMPDATA_TIMESTAMP ON RMPDATA" +
                " (\"TIMESTAMP\" ASC);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"RMPDATA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, RMPData entity) {
        stmt.clearBindings();
 
        Long timestamp = entity.getTimestamp();
        if (timestamp != null) {
            stmt.bindLong(1, timestamp);
        }
        stmt.bindDouble(2, entity.getValue());
 
        Long routeId = entity.getRouteId();
        if (routeId != null) {
            stmt.bindLong(3, routeId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, RMPData entity) {
        stmt.clearBindings();
 
        Long timestamp = entity.getTimestamp();
        if (timestamp != null) {
            stmt.bindLong(1, timestamp);
        }
        stmt.bindDouble(2, entity.getValue());
 
        Long routeId = entity.getRouteId();
        if (routeId != null) {
            stmt.bindLong(3, routeId);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public RMPData readEntity(Cursor cursor, int offset) {
        RMPData entity = new RMPData( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // timestamp
            cursor.getFloat(offset + 1), // value
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2) // routeId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, RMPData entity, int offset) {
        entity.setTimestamp(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setValue(cursor.getFloat(offset + 1));
        entity.setRouteId(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(RMPData entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(RMPData entity) {
        return null;
    }

    @Override
    public boolean hasKey(RMPData entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "rmpDataList" to-many relationship of RouteData. */
    public List<RMPData> _queryRouteData_RmpDataList(Long routeId) {
        synchronized (this) {
            if (routeData_RmpDataListQuery == null) {
                QueryBuilder<RMPData> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.RouteId.eq(null));
                queryBuilder.orderRaw("T.'TIMESTAMP' ASC");
                routeData_RmpDataListQuery = queryBuilder.build();
            }
        }
        Query<RMPData> query = routeData_RmpDataListQuery.forCurrentThread();
        query.setParameter(0, routeId);
        return query.list();
    }

}
