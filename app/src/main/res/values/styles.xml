<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>


    </style>

    <style name="greyButtonStyle">
        <item name="android:textColor">@color/colorDarkText</item>
        <item name="android:background">@drawable/rounded_grey_button</item>
    </style>

    <style name="registerEditText">
        <item name="android:background">@drawable/login_edit_text</item>
        <item name="android:paddingStart">@dimen/edit_text_padding</item>
        <item name="android:textSize">@dimen/text_size</item>
    </style>
</resources>
