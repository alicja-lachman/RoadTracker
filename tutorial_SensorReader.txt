Tworzenie obiektu klasy:

//np zmienna w Activity
SensorReader sensorReader;

//w metodzie
if(sensorReader==null)
            sensorReader = new SensorReader((SensorManager)getSystemService(SENSOR_SERVICE));



Zaczêcie zczytywania:
sensorReader.startSensorReading();


Zakoñczenie zczytywania:
sensorReader.finishSensorReadings();


Pobranie danych (w tym wypadku accelerometr):
List<SensorValues> accValues = sensorReader.getAccelerometerReadings();
